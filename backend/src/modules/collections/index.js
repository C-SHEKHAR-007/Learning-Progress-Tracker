/**
 * Collections module index
 */
const routes = require("./collection.routes");
const service = require("./collection.service");
const controller = require("./collection.controller");

module.exports = {
  routes,
  service,
  controller,
};
