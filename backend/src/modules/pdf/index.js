/**
 * PDF module index
 */
const routes = require("./pdf.routes");
const service = require("./pdf.service");
const controller = require("./pdf.controller");

module.exports = {
  routes,
  service,
  controller,
};
